# INGEST_SECRET is shared with the Yad2 ingest endpoint /api/ingest/yad2 — no separate secret.
INGEST_SECRET=changeme
PDIS_API_URL=https://pdis-lsah.onrender.com
FB_SCANS_PER_DAY=1

# Residential proxy — MANDATORY for personal FB account (datacenter IPs = ban)
# Get from Smartproxy, Bright Data, Oxylabs, IPRoyal — ~$5-15/month
# Format: http://username:password@proxy.provider.com:port
PROXY_URL=
