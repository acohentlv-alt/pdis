# PDIS — Task List
*March 31, 2026*

---

## DONE (today)

### Scheduled scans via external cron
**What:** Added `POST /api/scan/scheduled` endpoint with `CRON_SECRET` auth + background task execution. `GET /api/scan/status` shows if a scan is running. Boolean lock prevents overlapping scans. No APScheduler needed — use cron-job.org to trigger at 08:00/18:00 Israel time.

---

## AWAITING TEST

### Scheduled scan endpoints
**What:** Test the new `/api/scan/scheduled` (with and without auth) and `/api/scan/status` endpoints locally before deploying.

---

## NOT STARTED

### Create GitHub repo + push
**What:** Initialize a GitHub repo, push all 20+ local commits, connect to Render for auto-deploy.

### Deploy to Render
**What:** render.yaml exists. Connect repo to Render, verify the app works in production (Neon DB, static files, SPA routing). Then set `CRON_SECRET` env var and configure cron-job.org.

### Get Shechter's actual search presets
**What:** Current presets are placeholder Tel Aviv ranges. Need Shechter to specify his real neighborhoods, price ranges, and room counts. Once added, the scheduler picks them up automatically.

### Telegram bot (Phase 3b)
**What:** Send alerts when new hot properties are detected after a scan. Shechter gets a Telegram message with property summary + link.

### Facebook Marketplace integration (PARKED — reviewed, needs revision)
**What:** Add Facebook Marketplace Tel Aviv as third data source. Key value is image-based matching (perceptual hashing) to link same apartments across Facebook/Yad2/Madlan.
**Status:** Went through plan→review→revise→review cycle. Two reviews completed. Ready for final brief revision.
**Reviewer findings (must address before executing):**
1. Playwright won't work on Render free tier (needs Chromium ~400MB + system deps)
2. Image-based matching (dHash/pHash) is the critical feature — without it, Facebook data is a silo
3. Facebook without login yields only 5-20 listings (login wall after few scrolls)
4. Facebook preset INSERT needs `WHERE NOT EXISTS` guard
5. PropertyDetailPage source badge needs updating (not just PropertyCard)
6. Facebook image URLs expire — must hash at scrape time
7. Scope image comparison to active properties in same city
8. Use httpx instead of aiohttp (already a transitive FastAPI dep)
9. Exclude Facebook from removal detection (flaky scraper would mark listings as "removed")
